<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_window</item>
        <item name="android:statusBarColor">@color/bg_notibar</item>
    </style>

</resources>
